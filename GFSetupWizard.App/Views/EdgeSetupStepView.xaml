<UserControl x:Class="GFSetupWizard.App.Views.EdgeSetupStepView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="20">
                <TextBlock Text="Microsoft Edge Setup" 
                           FontSize="28" 
                           FontWeight="Bold" 
                           Margin="0,0,0,20"/>
                
                <TextBlock Text="Configure Microsoft Edge browser settings and enable syncing" 
                           FontSize="18" 
                           FontWeight="SemiBold" 
                           Foreground="#0078D7"
                           Margin="0,0,0,20"/>
                
                <TextBlock TextWrapping="Wrap" Margin="0,0,0,20">
                    Microsoft Edge is your primary web browser at GlobalFoundries.
                    Setting up Edge properly ensures your bookmarks, passwords, and preferences are synced across all your devices.
                </TextBlock>
                
                <Button x:Name="LaunchEdgeButton" 
                        Content="Launch Edge" 
                        Click="LaunchEdgeButton_Click"
                        Background="#0078D7"
                        Foreground="White"
                        Padding="15,10"
                        FontSize="14"
                        FontWeight="SemiBold"
                        HorizontalAlignment="Left"
                        Margin="0,0,0,30"/>
                
                <TextBlock Text="Setup Guide:" 
                           FontWeight="SemiBold" 
                           Margin="0,10,0,10"/>
                
                <StackPanel Margin="20,0,0,0">
                    <TextBlock Text="1. Click on your profile icon in the upper-left corner" Margin="0,5,0,5"/>
                    <TextBlock Text="2. Confirm syncing is ON" Margin="0,5,0,5"/>
                </StackPanel>
                
                <TextBlock TextWrapping="Wrap" Margin="0,20,0,20">
                    <Run FontWeight="SemiBold">Note:</Run> Syncing maintains bookmarks and preferences across your devices securely.
                    This ensures you have a consistent browsing experience on all your work devices.
                </TextBlock>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button x:Name="CompleteButton" 
                            Content="Yes, I've completed this step" 
                            Click="CompleteButton_Click"
                            Background="#4CAF50"
                            Foreground="White"
                            Padding="15,10"
                            Margin="0,0,10,0"/>
                    
                    <Button x:Name="SkipButton" 
                            Content="I'll do this later" 
                            Click="SkipButton_Click"
                            Background="#9E9E9E"
                            Foreground="White"
                            Padding="15,10"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
